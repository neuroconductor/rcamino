#' @title Wrapper for Camino \code{pointset2scheme} function
#' @description Performs the Camino \code{pointset2scheme} function
#' @export
pointset2scheme = function() {
  cmd = camino_cmd("pointset2scheme")
}
# .B pointset2scheme  [options]
#  
