#' @title Wrapper for Camino \code{counttracts} function
#' @description Performs the Camino \code{counttracts} function
#' @export
counttracts = function() {
  cmd = camino_cmd("counttracts")
}
# .B counttracts  -inputfile <tracts> [options]
