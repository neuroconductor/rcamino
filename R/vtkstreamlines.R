#' @title Wrapper for Camino \code{vtkstreamlines} function
#' @description Performs the Camino \code{vtkstreamlines} function
#' @export
vtkstreamlines = function() {
  cmd = camino_cmd("vtkstreamlines")
}
# .B vtkstreamlines [options]
