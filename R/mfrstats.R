#' @title Wrapper for Camino \code{mfrstats} function
#' @description Performs the Camino \code{mfrstats} function
#' @export
mfrstats = function() {
  cmd = camino_cmd("mfrstats")
}
# -voxels <num voxels> [options]
