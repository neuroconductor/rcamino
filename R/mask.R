#' @title Wrapper for Camino \code{mask} function
#' @description Performs the Camino \code{mask} function
#' @export
mask = function() {
  cmd = camino_cmd("mask")
}
# [-schemefile <filename> -bgthresh <positive integer>]
