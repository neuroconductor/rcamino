#' @title Wrapper for Camino \code{averagedwi} function
#' @description Performs the Camino \code{averagedwi} function
#' @export
averagedwi = function() {
  cmd = camino_cmd("averagedwi")
}
# .B averagedwi -inputfile <dwi> [options]
#  
