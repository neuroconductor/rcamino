#' @title Wrapper for Camino \code{reorient} function
#' @description Performs the Camino \code{reorient} function
#' @export
reorient = function() {
  cmd = camino_cmd("reorient")
}
# .B reorient -trans <transformation>
# [options]
