#' @title Wrapper for Camino \code{scheme2fsl} function
#' @description Performs the Camino \code{scheme2fsl} function
#' @export
scheme2fsl = function() {
  cmd = camino_cmd("scheme2fsl")
}
# .B scheme2fsl 
# -bscale <factor> [-flipx] [-flipy] [-flipz]
