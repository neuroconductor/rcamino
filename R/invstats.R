#' @title Wrapper for Camino \code{invstats} function
#' @description Performs the Camino \code{invstats} function
#' @export
invstats = function() {
  cmd = camino_cmd("invstats")
}
# -inversion <index> -voxels <num voxels>
