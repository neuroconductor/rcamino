#' @title Wrapper for Camino \code{sflutgen} function
#' @description Performs the Camino \code{sflutgen} function
#' @export
sflutgen = function() {
  cmd = camino_cmd("sflutgen")
}
# -infofile <filename> -outputfilestem <filename stem> [options]
