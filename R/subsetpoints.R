#' @title Wrapper for Camino \code{subsetpoints} function
#' @description Performs the Camino \code{subsetpoints} function
#' @export
subsetpoints = function() {
  cmd = camino_cmd("subsetpoints")
}
# .B subsetpoints  [options]
#  
