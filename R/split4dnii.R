#' @title Wrapper for Camino \code{split4dnii} function
#' @description Performs the Camino \code{split4dnii} function
#' @export
split4dnii = function() {
  cmd = camino_cmd("split4dnii")
}
# split4dnii -inputfile <file> -outputroot <root>
